"use client"

/**
 * Guide overlay disabled — the Fabric expansion frame is the single source of truth.
 * (Full-viewport SVG guides duplicated the frame and looked like extra extenders.)
 */
export default function ExpansionGuideOverlay() {
  return null
}
